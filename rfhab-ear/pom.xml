<?xml version="1.0" encoding="UTF-8" ?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>rfhab-ear</artifactId>
	<packaging>ear</packaging>

	<parent>
		<groupId>es.caib.rfhab</groupId>
		<artifactId>rfhab</artifactId>
		<version>1.0.2</version>
	</parent>

	<name>RFHab - Ear</name>

	<properties>
		<!-- Format del timestamp -->
		<maven.build.timestamp.format>dd/MM/yyyy HH:mm</maven.build.timestamp.format>
	</properties>

	<dependencies>

            <!-- Internes -->
			<dependency>
				<groupId>es.caib.rfhab.pluginsib</groupId>
				<artifactId>rfhab-pluginsib-rolsac</artifactId>
				<version>${project.version}</version>
			</dependency>

			<dependency>
				<groupId>es.caib.rfhab.pluginsib</groupId>
				<artifactId>rfhab-pluginsib-dir3caib</artifactId>
				<version>${project.version}</version>
			</dependency>
			
            <dependency>
                <groupId>es.caib.rfhab</groupId>
                <artifactId>rfhab-persistence</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>es.caib.rfhab</groupId>
                <artifactId>rfhab-ejb</artifactId>
                <version>${project.version}</version>
                <type>ejb</type>
            </dependency>

    		<dependency>
    			<groupId>es.caib.rfhab</groupId>
    			<artifactId>rfhab-back</artifactId>
    			<version>${project.version}</version>
    			<type>war</type>
    		</dependency>
    

            <!-- Tenir en compte a l'hora de incloure dependències només si és
            imprescindible 
			i no està ja dins un mòdul sobre el que es pugui establir una dependència 
			als descriptors.
            https://docs.jboss.org/author/display/AS72/Implicit+module+dependencies+for+deployments -->

            <!-- Externes -->
            

            <!-- ====================================== -->
            <!-- ===== PLUGINS DE USERINFORMATION ===== -->
            <!-- ====================================== -->

            <dependency>
                <groupId>org.fundaciobit.pluginsib.userinformation</groupId>
                <artifactId>pluginsib-userinformation-api</artifactId>
                <version>${pluginsib.userinformation.version}</version>
            </dependency>

            <dependency>
                <groupId>org.fundaciobit.pluginsib.userinformation</groupId>
                <artifactId>pluginsib-userinformation-keycloak</artifactId>
                <version>${pluginsib.userinformation.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.jboss.resteasy</groupId>
                        <artifactId>resteasy-client</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.jboss.resteasy</groupId>
                        <artifactId>resteasy-multipart-provider</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.jboss.resteasy</groupId>
                        <artifactId>resteasy-jaxrs</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.jboss.resteasy</groupId>
                        <artifactId>resteasy-jaxb-provider</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.jboss.resteasy</groupId>
                        <artifactId>resteasy-jackson2-provider</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>org.fundaciobit.pluginsib.userinformation</groupId>
                <artifactId>pluginsib-userinformation-database</artifactId>
                <version>${pluginsib.userinformation.version}</version>
            </dependency>
            <dependency>
                <groupId>org.fundaciobit.pluginsib.userinformation</groupId>
                <artifactId>pluginsib-userinformation-ldap</artifactId>
                <version>
                    ${pluginsib.userinformation.version}</version>
            </dependency>

                <!-- ============================================ -->
                <!-- ========== PLUGINS DE DATAEXPORT =========== -->
                <!-- ============================================ -->

                <!-- API -->
                <dependency>
                    <groupId>org.fundaciobit.pluginsib.exportdata</groupId>
                    <artifactId>pluginsib-exportdata-api</artifactId>
                    <version>${pluginsib.exportdata.version}</version>
                </dependency>


                <!-- Excel Plugin -->
                <dependency>
                    <groupId>org.fundaciobit.pluginsib.exportdata</groupId>
                    <artifactId>pluginsib-exportdata-excel</artifactId>
                    <version>${pluginsib.exportdata.version}</version>
                </dependency>

                <!-- ODS Plugin -->
                <dependency>
                    <groupId>org.fundaciobit.pluginsib.exportdata</groupId>
                    <artifactId>pluginsib-exportdata-ods</artifactId>
                    <version>${pluginsib.exportdata.version}</version>
                </dependency>

                <!-- CSV Plugin -->
                <dependency>
                    <groupId>org.fundaciobit.pluginsib.exportdata</groupId>
                    <artifactId>pluginsib-exportdata-csv</artifactId>
                    <version>${pluginsib.exportdata.version}</version>
                </dependency>

            <!-- Commons Dependencies -->
    		<dependency>
    			<groupId>commons-io</groupId>
    			<artifactId>commons-io</artifactId>
    		</dependency>
    
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
            </dependency>



                <!-- API REST de RFHab -->

                <!-- API EXTERNA START -->
                <!-- http://localhost:8080/rfhabapi/externa/index.html -->
    		<dependency>
    			<groupId>es.caib.rfhab</groupId>
    			<artifactId>rfhab-api-externa</artifactId>
    			<version>${project.version}</version>
    			<type>war</type>
    		</dependency>
                <!-- API EXTERNA END -->
                <!-- API INTERNA START -->
                <!-- http://localhost:8080/rfhabapi/interna/index.html -->
    		<dependency>
    			<groupId>es.caib.rfhab</groupId>
    			<artifactId>rfhab-api-interna</artifactId>
    			<version>${project.version}</version>
    			<type>war</type>
    		</dependency>
                <!-- API INTERNA END -->

	</dependencies>



	<build>
		<finalName>rfhab</finalName>

		<filters>
			<filter>../config.properties</filter>
		</filters>

		<plugins>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-ear-plugin</artifactId>

                    <!-- configuring the ear plugin -->
				<configuration>

					<displayName>RFHab</displayName>
					<description>RFHab</description>

					<generateApplicationXml>true</generateApplicationXml>
					<defaultLibBundleDir>lib</defaultLibBundleDir>
                    <skinnyWars>true</skinnyWars>
                    
                    <packagingExcludes>
						<!-- **/lib/resteasy-*.jar,
						**/lib/jackson-*.jar, -->
						**/bcprov-jdk15on-*redhat*.jar,
						**/bcmail-jdk15on-*redhat*.jar,
						**/bcpkix-jdk15on-*redhat*.jar,
						**/commons-codec-*redhat*.jar,
						**/commons-io-*redhat*.jar,
						**/commons-lang3-*redhat*.jar,
						**/jboss-annotations-api_1.3_spec-1.0.1.Final-redhat*redhat*.jar,
						**/jboss-jaxb-api_2.3_spec-1.0.1.Final-redhat*redhat*.jar,
						**/activation*redhat*.jar,
						**/jboss-annotations-api_1.3_spec*redhat*.jar,
						**/jboss-jaxb-api_2.3_spec*redhat*.jar,
						**/commons-logging-jboss-logging*redhat*.jar,
						**/slf4j-api*redhat*.jar,
                        <!-- Requereix afegir mòdul 'com.fasterxml.jackson.datatype.jackson-datatype-jsr310' a jboss-deployment-structure.xml -->
						**/jackson-datatype-jsr310*redhat*.jar,
                        <!-- Requereix afegir mòdul 'com.github.fge.json-patch' a jboss-deployment-structure.xml -->
						**/json-patch*redhat*.jar,
                        <!-- Requereix afegir mòdul 'javax.ws.rs.api' a jboss-deployment-structure.xml -->
						**/jboss-jaxrs-api_2.1_spec*redhat*.jar,
                        <!-- Requereix afegir mòdul 'org.jboss.resteasy.resteasy-jackson2-provider' a jboss-deployment-structure.xml -->
						**/resteasy-jackson2-provider*redhat*.jar,
						**/jboss-logging*redhat*.jar,
                        <!-- Requereix afegir mòdul 'org.apache.log4j' a jboss-deployment-structure.xml -->
						**/log4j-*.jar,
                        <!-- Requereix afegir mòdul 'javax.ws.rs.api' a jboss-deployment-structure.xml -->
						**/jsr311-api-1.1.1.jar
					</packagingExcludes>
                    
					<includeLibInApplicationXml>false</includeLibInApplicationXml>
                        <!--suppress UnresolvedMavenProperty -->
                        <!-- Nom mòduls d'acord amb punt 4.2 de l'Estàndard CAIB -->
					<outputFileNameMapping>@{artifactId}@-@{version}@.@{extension}@</outputFileNameMapping>
				

					<archive>
						<manifest>
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
						</manifest>
						<manifestEntries>
							<project-version>${project.version}</project-version>
							<project-buildtime>${maven.build.timestamp}</project-buildtime>
							<scm-revision>${buildNumber}</scm-revision>
						</manifestEntries>
					</archive>

					<defaultLibBundleDir>lib</defaultLibBundleDir>

					<modules>

						<webModule>
							<groupId>es.caib.rfhab</groupId>
							<artifactId>rfhab-back</artifactId>
							<contextRoot>rfhabback</contextRoot>
                            <bundleFileName>rfhab-back.war</bundleFileName>
						</webModule>
                        
                        

						<ejbModule>
							<groupId>es.caib.rfhab</groupId>
							<artifactId>rfhab-ejb</artifactId>
							<bundleFileName>rfhab-ejb.jar</bundleFileName>
						</ejbModule>

                                <!-- API EXTERNA START -->
						<webModule>
							<groupId>es.caib.rfhab</groupId>
							<artifactId>rfhab-api-externa</artifactId>
							<contextRoot>/rfhabapi/externa</contextRoot>
							<bundleFileName>rfhab-api-externa.war</bundleFileName>
						</webModule>
                                <!-- API EXTERNA END -->
                                <!-- API INTERNA START -->
						<webModule>
							<groupId>es.caib.rfhab</groupId>
							<artifactId>rfhab-api-interna</artifactId>
							<contextRoot>/rfhabapi/interna</contextRoot>
							<bundleFileName>rfhab-api-interna.war</bundleFileName>
						</webModule>
                                <!-- API INTERNA END -->
					</modules>

				</configuration>
			</plugin>

                <!-- Per arrencar/desplegar automàticament l'aplicació. 
			     Documentació ús: https://codehaus-cargo.github.io/cargo/Maven2+plugin.html 
				configuració: https://codehaus-cargo.github.io/cargo/Maven2+Plugin+Reference+Guide.html 
		   -->
			<plugin>
				<groupId>org.codehaus.cargo</groupId>
				<artifactId>cargo-maven2-plugin</artifactId>
				<configuration>
					<skip>false</skip>
					<container>
                            <!-- El plugin cargo empra la configuració wildfly13x pel JBoss EAP 
							7.2 -->
						<containerId>wildfly13x</containerId>
						<type>installed</type>
						<home>${env.JBOSS_HOME}</home>
					</container>
					<configuration>
                            <!-- Empra la configuració existent del servidor d'aplicacions. -->
						<type>existing</type>
						<home>${env.JBOSS_HOME}/standalone</home>
                            <!-- Si volem arrancar el jboss amb cargo:start i amb una configuració 
							copiada de l'original, fixar el <type> a standalone i llevar el <home> -->
						<properties>
							<cargo.hostname>localhost</cargo.hostname>
							<cargo.port.offset>0</cargo.port.offset>
							<cargo.remote.username>admin</cargo.remote.username>
							<cargo.remote.password>admin</cargo.remote.password>
						</properties>
					</configuration>
					<deployables>
						<deployable>
							<properties>
								<name>${project.build.finalName}</name>
							</properties>
						</deployable>
					</deployables>
				</configuration>
			</plugin>

		</plugins>
	</build>
	
	
    <profiles>
        <profile>
          <id>revisionnumber</id>
          <build>
            <plugins>
                        <!-- Defineix la propietat ${buildNumber} que s'empra al filtrat -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>buildnumber-maven-plugin</artifactId>
                </plugin>
            </plugins>
          </build>
        </profile>
    </profiles>

</project>
